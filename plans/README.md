<!-- markdownlint-disable MD013 -->

# Implementation Plans

Generated by the improve skill on 2026-08-06. Execute these plans in the
listed order unless a dependency says otherwise. Each executor must read its
plan fully, obey its STOP conditions, and update its status row.

## Execution order and status

| Plan | Title | Priority | Effort | Depends on | Status |
| --- | --- | --- | --- | --- | --- |
| 001 | Add a mise-managed Cargo advisory gate | P1 | S | — | DONE |
| 002 | Keep daemon mutations in durable commit order | P1 | M | — | DONE |
| 003 | Make event-log compaction use one current key cut | P1 | M | 002 | DONE |
| 004 | Keep private settings out of hook dry-run output | P1 | S | — | DONE |
| 005 | Reject unsupported state versions before use | P1 | M | — | DONE |
| 006 | Bound daemon client request resources | P1 | M | 002 | DONE |
| 007 | Verify release artifact provenance before install | P2 | M | — | DONE |
| 008 | Identify amux-owned hook commands exactly | P2 | M | 004 | DONE |
| 009 | Use collision-safe tmux field encoding | P2 | M | — | DONE |
| 010 | Pin the Cargo version editor | P2 | S | 001 | DONE |
| 011 | Replace the obsolete release checklist | P2 | S | 001, 007, 010 | DONE |
| 012 | Test daemon retirement after binary replacement | P2 | M | 002 | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line reason)

## Dependency notes

- Plan 001 is first because the user explicitly requested `cargo-audit` with
  mise before the other work.
- Plan 003 follows Plan 002 because both plans change daemon mutation order.
  The compaction test must use the final mutation boundary.
- Plan 006 follows Plan 002 so its handler-limit work does not hide or restore
  the old publication race.
- Plan 008 follows Plan 004 because both plans change hook ownership and
  preview behavior in `src/hooks.rs`.
- Plan 010 follows Plan 001 because both plans add Cargo tools to `mise.toml`.
- Plan 011 follows Plans 001, 007, and 010 so the checklist describes the
  final security and release commands.
- Plan 012 follows Plan 002 so its daemon integration test uses the final
  mutation coordinator.

## Findings considered and rejected

- Projection indexing: rejected because `ISSUES.md` records a measured 1.47 ms
  projection time and rejects indexing below the 5 ms threshold.
- Timers or process polling for missing Pi and opencode signals: rejected
  because `docs/events.md` records those gaps as upstream limitations.
- Report the canonical TPM launcher as drift: rejected because
  `~/.tmux/plugins/amux/bin/amux` is the expected production path.
- Add a Cargo CI cache now: deferred because the audit had no CI timing data.
  Measure clean and warm jobs before a cache plan.
- Replace daemon idle polling now: deferred because the audit had no measured
  CPU or power cost. First measure an idle daemon with representative
  subscribers.
- Make all Rust modules private now: deferred because external crate use is
  unknown. First decide whether amux is a supported Rust library.
- Quote the optional `next-attention` path as a separate plan: rejected as a
  low-value standalone change. The canonical TPM path is stable; include this
  small change in a later tmux packaging change if path-with-space support is
  required.
- Add a separate daemon-start-failure fallback test: deferred because the
  current direct-write tests cover the durable result and higher-risk daemon
  concurrency tests come first.
